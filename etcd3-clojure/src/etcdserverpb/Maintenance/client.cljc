;;;----------------------------------------------------------------------------------
;;; Generated by protoc-gen-clojure.  DO NOT EDIT
;;;
;;; GRPC etcdserverpb.Maintenance Client Implementation
;;;----------------------------------------------------------------------------------
(ns etcdserverpb.Maintenance.client
  (:require [etcdserverpb :refer :all]
            [authpb :as authpb]
            [mvccpb :as mvccpb]
            [clojure.core.async :as async]
            [protojure.grpc.client.utils :refer [send-unary-params invoke-unary]]
            [promesa.core :as p]
            [protojure.grpc.client.api :as grpc]))

;-----------------------------------------------------------------------------
; GRPC Client Implementation
;-----------------------------------------------------------------------------

(defn Alarm
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "Alarm"
              :input   {:f etcdserverpb/new-AlarmRequest :ch input}
              :output  {:f etcdserverpb/pb->AlarmResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

(defn Status
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "Status"
              :input   {:f etcdserverpb/new-StatusRequest :ch input}
              :output  {:f etcdserverpb/pb->StatusResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

(defn Defragment
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "Defragment"
              :input   {:f etcdserverpb/new-DefragmentRequest :ch input}
              :output  {:f etcdserverpb/pb->DefragmentResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

(defn Hash
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "Hash"
              :input   {:f etcdserverpb/new-HashRequest :ch input}
              :output  {:f etcdserverpb/pb->HashResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

(defn HashKV
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "HashKV"
              :input   {:f etcdserverpb/new-HashKVRequest :ch input}
              :output  {:f etcdserverpb/pb->HashKVResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

(defn Snapshot
  [client params reply]
  (let [input (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "Snapshot"
              :input   {:f etcdserverpb/new-SnapshotRequest :ch input}
              :output  {:f etcdserverpb/pb->SnapshotResponse :ch reply}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (grpc/invoke client desc))))))

(defn MoveLeader
  [client params]
  (let [input (async/chan 1)
        output (async/chan 1)
        desc {:service "etcdserverpb.Maintenance"
              :method  "MoveLeader"
              :input   {:f etcdserverpb/new-MoveLeaderRequest :ch input}
              :output  {:f etcdserverpb/pb->MoveLeaderResponse :ch output}}]
    (-> (send-unary-params input params)
        (p/then (fn [_] (invoke-unary client desc output))))))

